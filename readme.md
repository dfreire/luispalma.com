- Kirby's `panel` and `kirby` directories are excluded, they should be installed with the kirby cli.
- Kirby's `thumbs`, `site/cache`, and `site/accounts` directories are excluded, creating them may be necessary.
